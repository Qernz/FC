#include <iostream>

using namespace std;
srand();
float markov(float x0, float r, int m, float (*p)(float), float n, )
{
  float *cm = new float[n];
  
  for (int i = 0; i< n; i++)
  {
    cm[i] = metropolis(x0, r, m, )
  }






}
